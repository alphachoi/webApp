server {
    listen      80;
    server_name localhost;
    charset     utf-8;

    location /static {
        root /srv/webApp/www/static;
    }

    location / {
        uwsgi_pass  127.0.0.1:9001;
        include     uwsgi_params;
    }
}
