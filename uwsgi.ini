[uwsgi]

chdir = /var/webApp/www

#python module to import
app = wsgi
module = %(app)
callable = applicaton



#virtualenv folder
virtualenv = /var/webApp/venv

master = true

#socket file's location
socket = /var/webApp/mysite.sock

#permissions for the socket file
chmod-socket = 666

vacuum = true
